<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Forgot Username</title>
  <link rel="stylesheet" href="/css/login.css">
</head>
<body>

  <div class="loggin-msg">
    <% if (success) { %>
      <div><%= success %></div>
    <% } %>
    
    <% if (error) { %>
      <div style="color: red; font-weight: bold; margin-bottom: 10px;"><%= error %></div>
    <% } %>
  </div>
      

  <div class="login-wrapper">
    <div class="login-box">
      <h2>Forgot Username</h2>


      <% if (!showForm) { %>
        <form method="POST" action="/forgot-username">
          <label class="forgot-username-label">Company Name</label>
          <input type="text" name="companyName" required>
      
          <label class="forgot-username-label">Enter your admin password</label>
          <input type="password" name="password" required>
      
          <button type="submit" class="Verify-pass">Verify Password</button>
        </form>
      <% } else { %>
        <form method="POST" action="/update-username">
          <input type="hidden" name="companyName" value="<%= companyName %>">
          <label class="forgot-username-label">Create new username</label>
          <input type="text" name="newUsername" required>
          <button type="submit" class="save-username">Save Username</button>
        </form>
      <% } %>
      

      <div></div>
      

      <br><a href="/login" class="bck-to-login"> ← Back to Login</a>
    </div>
  </div>
</body>
</html>
